pipeline {
    agent any

    stages {
        stage('Test Python') {
            steps {
                bat 'python --version'
                bat 'echo Jenkins CI OK'
            }
        }
    }
}
